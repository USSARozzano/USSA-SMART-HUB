services:
  - type: web
    name: ussa-smart-hub-v2
    env: python
    buildCommand: pip install -r requirements.txt && python build_routes.py
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
